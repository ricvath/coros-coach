# MAC
.DS_Store

# node
node_modules
dist

# credentials — never commit real credentials
coros.config.json
.coros-token/

# local artifacts
*.txt
*.zip

# planning / session notes (local only)
.planning/